# 🔄 Инструкция: Обновление API URL после деплоя Backend

Когда backend задеплоен на Render, вы получите URL вида:
```
https://uniguide-backend.onrender.com
```

## ✅ Метод 1: Обновить config.js (Рекомендуется)

Откройте файл `config.js` и замените:

```javascript
const API_CONFIG = {
    // Замените на ваш Render URL:
    BASE_URL: 'https://uniguide-backend.onrender.com/api',
    // ...
};
```

Затем подключите config.js в HTML файлы:

### В index.html:
Добавьте перед закрывающим `</body>`:
```html
<script src="config.js"></script>
<script>
    // Используйте window.API_CONFIG.BASE_URL
    const API_BASE_URL = window.API_CONFIG.BASE_URL;
    // ... остальной код
</script>
```

### В result.html:
То же самое:
```html
<script src="config.js"></script>
<script>
    const API_BASE_URL = window.API_CONFIG.BASE_URL;
    // ... остальной код
</script>
```

---

## 🔧 Метод 2: Прямая замена в каждом файле

Если не хотите использовать config.js, найдите в каждом файле:

### index.html:
```javascript
// Найти:
const API_BASE_URL = 'http://127.0.0.1:8000/api';

// Заменить на:
const API_BASE_URL = 'https://uniguide-backend.onrender.com/api';
```

### result.html:
```javascript
// Найти:
const API_BASE_URL = 'http://127.0.0.1:8000/api';

// Заменить на:
const API_BASE_URL = 'https://uniguide-backend.onrender.com/api';
```

---

## ✅ Проверка

После обновления:

1. Откройте `index.html` в браузере
2. Откройте DevTools (F12) → Console
3. Введите тестовый запрос в поиск
4. Проверьте, что результаты приходят без ошибок

Если видите ошибки CORS:
- Проверьте настройки CORS в backend (`app/main.py`)
- Убедитесь, что backend запущен и доступен

---

## 🚀 Быстрая проверка backend

Откройте в браузере:
```
https://uniguide-backend.onrender.com/api/health
```

Должно вернуть:
```json
{"status": "ok"}
```

Если ошибка "Service Unavailable":
- Backend еще запускается (подождите 1-2 минуты)
- Или был неудачный деплой (проверьте логи на Render)

---

## 📝 Checklist

- [ ] Backend задеплоен на Render
- [ ] Получен URL backend
- [ ] Обновлен API_BASE_URL во frontend
- [ ] Проверен `/api/health` endpoint
- [ ] Протестирован поиск через frontend
- [ ] Проверены результаты на странице result.html

---

🎉 **Готово!** Теперь ваш frontend подключен к реальному backend API!
